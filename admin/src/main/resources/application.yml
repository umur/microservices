server:
  port: 8093

spring:
  application:
    name: admin
#  boot:
#    admin:
#      discovery:
#        ignored-services: admin


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true


